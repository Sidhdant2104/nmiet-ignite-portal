import { createFileRoute, Link } from "@tanstack/react-router";
import { Button } from "@/components/ui/button";
export const Route = createFileRoute("/submission-guidelines")({component:Guidelines});
function Guidelines(){return <div className="mesh-bg min-h-dvh pt-28 pb-16"><section className="shell max-w-4xl"><p className="text-xs font-bold uppercase tracking-[.18em] text-primary">PPT submission</p><h1 className="mt-2 font-display text-4xl font-bold">Submission Guidelines</h1><div className="mt-8 grid gap-5 md:grid-cols-2">{[["Accepted formats",".ppt, .pptx, or .pdf only."],["Maximum size","25 MB per upload."],["Naming convention","ReferenceID_TeamName_VersionNumber (example: SIH2026-0001_Innovators_V1.pptx)."],["Mandatory slides","Title, problem understanding, solution, innovation, technical approach, implementation/prototype, impact, and team details."],["Submission rules","Use the leader email and reference ID to verify. Uploads are versioned; the latest valid version is reviewed. Do not share your verification link."],["Important deadlines","The deadline shown in the submission portal is final. Re-uploads are allowed only before it."]].map(([title,body])=><article key={title} className="rounded-2xl border bg-card p-5"><h2 className="font-display text-lg font-bold">{title}</h2><p className="mt-2 text-sm text-muted-foreground">{body}</p></article>)}</div><Link to="/ppt-submission"><Button className="mt-8">Proceed to secure upload</Button></Link></section></div>}
